services:
  - type: web
    name: restxqr-backend
    env: node
    rootDir: /
    buildCommand: npm install
    startCommand: node src/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
